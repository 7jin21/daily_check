# Inner Mirror 利用手順書（MVP）

## 1. 起動
1. 依存関係をインストール: `npm install`
2. 開発サーバー起動: `npm run dev`
3. ブラウザで表示されたURLを開く。

## 2. 基本操作

### 2.1 日記を新規作成
1. ホーム画面で「今日の日記を書く」を押す。
2. 6ステップで入力する。
   - 気分
   - エネルギー
   - 今日あったこと
   - 困ったこと
   - 感謝していること
   - 自由記述
3. 最後の「次へ」でドラフト生成。
4. 「編集する」で本文を修正可能。
5. 「保存する」で当日の記録を保存。

### 2.2 過去の日記を見る
1. ホームから「日記一覧」を開く。
2. 見たい日付カードをクリック。
3. 詳細画面で本文を確認。

### 2.3 インサイトを見る
1. ホームから「インサイト」を開く。
2. 「AIにパーソナリティを分析してもらう」を押す。
3. 性格特性/思考パターン/感情/価値観/アドバイスを確認。

## 3. データ保存仕様
- 一覧データ: `diary:index`
- 日次データ: `diary:entry:YYYY-MM-DD`
- `window.storage` があればそれを利用、なければメモリ上で保持。

## 4. 注意事項
- APIキー未設定時はオフライン生成（ローカルロジック）で動作。
- 本番運用では APIキーをクライアントへ埋め込まないこと。
