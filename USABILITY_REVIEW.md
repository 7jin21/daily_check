# Inner Mirror ユーザビリティ/機能確認メモ

## 結論
現状のMVPは「日次記録を短時間で残す」という目的に対して**基本的に利用可能**です。
一方で、継続率と信頼性を高めるには、API連携とエラーハンドリングの実装強化が必要です。

## 確認観点と評価

- チェックイン導線: ホームから1タップで開始でき、ステップ構成も明確（良好）。
- 入力負荷: mood/energyの選択式 + 自由入力の組み合わせで低負荷（良好）。
- 生成体験: ドラフト生成/再生成/編集/保存の流れが一通り成立（良好）。
- データ保持: `diary:index` と `diary:entry:{date}` で閲覧・詳細表示が可能（良好）。
- 空状態対応: 一覧/インサイトで初期メッセージがあり迷いにくい（改善済み）。
- エラー耐性: APIキー未設定時はオフライン生成へフォールバック（改善済み）。

## 追加推奨（優先度順）
1. APIキーをクライアント保持しない構成（BFF/API Route）へ移行。
2. 生成・分析中のキャンセル操作とリトライUIを追加。
3. Notion/GCal/Gmail連携の接続状態を設定画面で可視化。
4. 音声入力の実装（Web Speech API）とブラウザ対応分岐。
5. 週次/月次レポートの集計UIを追加。
